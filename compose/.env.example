# Copied to compose/.env (gitignored). Used for compose port interpolation.
# Default loopback. Prefer host port 8787. Do not publish on a host port already
# used by another service; avoid host 8080 when something else owns it.
RUNNER_HOST=127.0.0.1
RUNNER_PORT=8787
